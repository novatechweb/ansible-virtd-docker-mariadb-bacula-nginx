{% if gitlab_docker_backup_dir is defined %}

# *****************************************************************************
# Clean up git
docker run --rm -v {{ docker_backup_dir }}:/tmp/import_export debian:production bash -c "rm -rf /tmp/import_export/GIT"
{% endif %}
