---
# passwords
credentials_base_dir: "~/Workbench/ansible-virtd-docker-mariadb-bacula-nginx/"
mariadb_root_password:               '{{ lookup("password", credentials_base_dir + "credentials/" + inventory_hostname + "/mariadb/root length=20") }}'
bacula_director_password:            '{{ lookup("password", credentials_base_dir + "credentials/" + inventory_hostname + "/bacula/director length=20") }}'
bacula_monitor_password:             '{{ lookup("password", credentials_base_dir + "credentials/" + inventory_hostname + "/bacula/monitor length=20") }}'
mariadb_bacula_password:             '{{ lookup("password", credentials_base_dir + "credentials/" + inventory_hostname + "/bacula/mariadb length=20") }}'
wiki_db_root_password:               '{{ lookup("password", credentials_base_dir + "credentials/" + inventory_hostname + "/" + container_addr_map.wiki.hostname + "/root length=20") }}'
wiki_db_password:                    '{{ lookup("password", credentials_base_dir + "credentials/" + inventory_hostname + "/" + container_addr_map.wiki.hostname + "/" + wiki_db_user + " length=20") }}'
gitlab_db_password:                  '{{ lookup("password", credentials_base_dir + "credentials/" + inventory_hostname + "/" + container_addr_map.git.hostname + "/" + gitlab_db_user + " length=20") }}'
buildsystem_ssh_prod_user_password:  '{{ lookup("password", credentials_base_dir + "credentials/buildsystem/ssh/prod length=20") }}'
buildsystem_ssh_root_user_password:  '{{ lookup("password", credentials_base_dir + "credentials/buildsystem/ssh/root length=20") }}'
buildsystem_database_root_password:  '{{ lookup("password", credentials_base_dir + "credentials/buildsystem/database/root length=20") }}'
buildsystem_database_build_password: '{{ lookup("file",     credentials_base_dir + "credentials/buildsystem/database/readuser") }}'
svn_username:                        '{{ lookup("file",     credentials_base_dir + "personal_credentials/svn/username") }}'
svn_password:                        '{{ lookup("file",     credentials_base_dir + "personal_credentials/svn/password") }}'